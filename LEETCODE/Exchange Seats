WITH CTE AS (
  SELECT MAX(ID) as lastid
  FROM Seat
)
SELECT CASE 
WHEN id = (SELECT lastid FROM CTE) AND id % 2 <> 0 THEN id
WHEN id % 2 <> 0 THEN  id+1
WHEN id % 2 = 0 THEN  id-1
END as id, student
FROM Seat
ORDER BY id 
